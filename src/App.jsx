import React from "react";

// Router
import Router from "./Router";

// Css
import "./assets/css/style.css";

/**
 * App component that renders the Router component, which handles the application's routing.
 *
 * @returns {JSX.Element} JSX element that represents the App component and its child components.
 */
function App() {
    return <Router />;
}

export default App;
